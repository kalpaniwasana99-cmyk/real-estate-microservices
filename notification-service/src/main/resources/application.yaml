server:
  port: 8084

spring:
  application:
    name: notification-service
  data:
    mongodb:
      # Environment variable එකක් තිබේ නම් එය පාවිච්චි කරයි, නැතහොත් ඩිෆෝල්ට් ලෝකල් යූආර්එල් එක ගනී
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/real_estate_db}
      database: ${SPRING_DATA_MONGODB_DATABASE:real_estate_db}
  mail:
    host: smtp.gmail.com
    port: 587
    username: kaushbasnayake@gmail.com
    password: qrvqwwbxghvbkklp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

notification:
  security:
    api-key: notify-service-secret-key-123

springdoc:
  swagger-ui:
    path: /swagger-ui.html